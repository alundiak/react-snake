import React from 'react';
import { Board } from './Board';
// import { Point } from './Point';
// import { Snake } from './Snake';

import './snake-game.css';
// SnakeComponent(x1,y1, x2, y2, memoryLength)
// matrix[0] => matrix[max]
// matrix[0] => matrix[max]
// SnakeGameComponent (while/for, logika/conditions for exit, conflict(wall/x/y, albo self))

// OR random   Math.random(10, 0);
export function SnakeGame() {
  // dynamics, re-renders, etc
  // console.log(Board);
  return (
    <Board>
      <div>kurwa</div>
      {/* <Point /> */}
      {/* <Snake /> */}
    </Board>
  );
}
